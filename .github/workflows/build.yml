name: Rebuild data JSON

on:
  push:
    paths:
      - "data/questions.csv"
      - "data/toppers.overrides.json"
      - "build.js"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node build.js
      - name: Commit regenerated files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/copies.json data/index.json data/toppers.json toppers.html sitemap.xml llms.txt robots.txt index.html
          git diff --cached --quiet || git commit -m "chore: rebuild data + static/SEO artefacts [skip ci]"
          git push
